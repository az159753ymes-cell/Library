# 班級書箱管理系統 使用者操作手冊

本手冊旨在引導圖書館管理教師、行政人員及相關協助同學，輕鬆操作「班級書箱管理系統」。

---

## 壹、 快速入門說明

1. **開啟系統**：
   * 雙擊桌面的「開啟圖書館系統」捷徑；系統會自動以 Google Chrome 開啟本機預覽網址。
   * 請勿直接雙擊 `index.html`，因為 Google 登入與系統資源需透過本機網頁服務載入。
2. **免安裝與網路說明**：
   * 「本機預覽」與「公開網站」使用同一份 Firestore 正式資料。管理員修改書箱、輪換或設定後，請點左上角 LIBRARY 圖樣，於右側狀態卡再按「確認儲存」；完成前教師端不會看到變更。
   * 本機預覽只用來查看尚未上傳的程式畫面與功能。未上傳的 HTML、樣式與圖片不會出現在公開網站。
   * 左下角小女孩手上的資料夾一律下載完整的正式 Firestore JSON 備份。若將進行大型程式修改，系統管理者會先建立可還原的專案壓縮備份。
   * 確認本機預覽無誤後，直接告知系統管理者「上傳到GitHub」。系統會建立版本、推送 GitHub，並由 GitHub Pages 更新公開網站。

3. **首次公開設定**：
   * GitHub 儲存庫需設為公開，並在「Settings → Pages」選用「GitHub Actions」作為發布來源。
   * 公開網址為 `https://az159753ymes-cell.github.io/Library/`。
   * 在 Firebase Console 的「Authentication → Settings → Authorized domains」加入 `az159753ymes-cell.github.io`，否則公開網站無法完成 Google 登入。
   * 完成上述一次性設定後，日後只需依本手冊的「上傳到GitHub」流程更新公開網站。

---

## 貳、 主畫面與系統選單導覽

### 一、 非管理者教師選書

1. 所有人都需先使用 Google 帳號登入。系統會依帳號身分顯示不同畫面：管理員進入管理工作臺；非管理員進入教師選書頁面。
2. 教師若要登出，按右上角帳號名稱旁的「登出」；下一次登入會開啟 Google 帳號清單，可選擇正確教育帳號。
3. 教師選書在開放時段內可即時完成；若同一時段的書箱已被其他班級選定，或同班已選過該書箱，系統會保留原有選擇並提示原因。登入頁面會顯示「請使用瀏覽器開啟連結，不要在LINE直接點選！」提醒；手機教師登入後另會出現中央紅字提示，提醒選書已自動儲存並於右上角登出。
   * 教師選書固定對應學期管理中最後新增的學期。若時段尚未設定或尚未到開始時間，非管理員登入後中央顯示「１１Ｘ學年度第Ｘ學期「班級書箱」選書時段尚未開放」，不顯示班級輸入與選書表。
   * 開放時段截止後，中央會顯示「１１Ｘ學年度第Ｘ學期「班級書箱」選書開放時段已截止」。
2. 先輸入本次選書的3位數班級代碼，例如 `701`，再按「進入書箱選擇」。每次登入都要重新輸入班級；進入後頁首會顯示如「701書箱選擇」。
3. 選書表會保留全部書箱，並將書箱資料拆分為「書箱編號」、「書箱名稱」與「書箱總數」三欄，右側為3個閱讀時段。每段日期以「收到書箱日」起算，至下一批圖資股長收書日當日止；表格資料列統一使用白底，每一格顯示：
   * **可選**：該時段尚未有班級選取，點擊後即可選定。
   * **班級號碼**：該時段已由該班選取。
   * **留白**：書箱有缺號，或管理者已關閉星星暫停輪換；三個時段會留白，書箱仍會顯示在表格中，但不能選擇或匯入輪換。
4. 同一本書箱可在3個不同閱讀時段分別由不同班級選取；同一時段僅第一位成功送出的教師可選定。同一班級每個時段只能選1箱，且同一學期不會重複選同一本書。每次成功選取都會寫入本次輸入的班級代碼。確認班級後，系統會優先顯示該班既有選書，不必等待全校選書資料完全載入。
5. 選書開放時段內，如欲更換同一時段的書箱，直接點選另一個「可選」書箱。系統會先顯示原書箱與新書箱，確認後才改選；取消則保留原選擇。
6. **班級優先調整權限**：任何已登入教師只要輸入相同的3位數班級代碼，即可接手調整該班既有選書，不受最初選書所用 Google 帳號限制。請務必確認輸入的班級正確。
7. 如決定不指定某個時段的書箱，點選該格深綠班級標籤右側的磚紅色「×」圓角標籤。系統會立即取消該班在此時段的指定，該格恢復「可選」；之後由班際輪換流程分配，不會匯入為教師指定書籍。
7. 需要登出或切換帳號時，點選右上角的「Google 顯示名稱 登出」。

### 二、 管理者工作臺

僅已驗證的 Google 帳號 `az159753ymes@gmail.com` 可使用管理員工作臺。登入後可操作書箱清冊、班際輪換與教師選擇；畫面不會標示「管理員」。若要登出或切換帳號，請點選左下角的小女孩插圖。

* **儲存與教師端更新**：管理頁載入與重新整理只讀取 Firestore，不會自動寫入。完成修改後點左上角可互動的 LIBRARY 圖樣；右側狀態卡顯示「尚未儲存；教師端尚未更新」時，再按「確認儲存」，才會儲存正式資料並讓教師端取得變更。沒有修改時點 LOGO 只顯示「目前資料已儲存」，不會寫入 Firestore。此入口不會變更您已設定的教師選書開放與截止時間；未儲存時請勿直接重新整理或關閉頁面。

* 管理者登入後於「教師選書」右上角點選「開放時段」，即可設定或在截止後修正教師選書的開始與截止時間。視窗剛開啟時，請先等按鈕變成「儲存時段」；若仍顯示「尚未讀取完成」，可以點按鈕，系統會彈窗說明原因。按下「儲存時段」後，系統只會寫入教師入口所需的目前學期、班級清單與時段，並從正式 Firestore 讀回同一組時間；成功時會彈窗明確告知已寫入正式資料庫，之後視窗才會關閉。失敗時會保留日期與時間，並以彈窗顯示原因與錯誤資訊。
* **紀錄清理**：先點左下角資料夾下載完整正式 JSON 備份，再於「教師選書」右上角點「紀錄清理」，選擇學期並輸入完整學期名稱確認。清理已結束學期時會清除完整教師選書來源；清理當學期時會清除教師選書資料，保留教師入口、書箱、開放時段與班際輪換。系統不會區分「測試」與「正式」選書。
* 時段截止後，系統會自動匯入合格教師選擇至「班際輪換」，並標示為教師來源；缺號或關閉星星的書箱不會匯入。選書開放期間不提供手動匯入。
* 點選教師指定書籍時，系統會先詢問是否編輯此項班書，確認後才會開啟編輯。
* 管理者按下「隨機」時，教師選定與手動指定的書箱均會保留；系統只依原有規則分配其餘未指定的格位。
* 左下角小女孩手上的資料夾：無論在本機預覽或公開網站，都會下載完整 Firestore JSON 備份；只限管理員使用。

* **左側側邊欄**：
  * **【書箱清冊】**：管理圖書館現有各大書箱之編號、書名、缺號與數量。手機版採固定左右雙欄：左側為書籍卡片，右側為新增／修改表單，不需橫向滑動；所有按鈕功能與桌機相同。
  * **【班際輪換】**：規劃 7年級 與 8年級 各班每學期的三階段班級書箱輪流借閱排程。
* **右上角學期選擇器**：
  * 專用於「班際輪換」頁面右上角，可切換「114學年度 第一學期」、「114學年度 第二學期」等。
  * 「書箱清冊」頁面隱藏學期選擇器，使清冊介面更加簡潔專注。
  * 切換學期可隔離不同學期的班輪排程資料。

---

## 參、 功能操作指引

### 一、 書箱清冊管理模組

1. **瀏覽書箱清冊**：
   * 點擊左側「書箱清冊」，中央表格會列出當前學期所有的書箱詳細資料。
2. **新增書箱資料**：
   * 點擊右上角 **「＋ 新增書箱」** 按鈕。
   * 右側表單會切換為**淡綠色背景主題**以方便識別，輸入以下資訊：
   * 【編號】只能輸入數字；全形數字會自動轉為半形，中文與符號不會保留。
     * **【編號】**（必填）：如 `1`（支援全形數字自動轉半形）。
     * **【書名】**（必填）：請輸入純文字書名（系統會自動整理格式）。
     * **【缺號】**（選填）：若有缺本，請輸入缺號數字（有多本缺號時請用空格分開，例如：`23 21`）。
     * **【數量】**（必填）：填寫「書本到幾號」，系統將依據缺號本數自動算出「共幾本」。
     * **【備註】**（選填）：可記錄圖書狀況或借閱註記（如：`絕版` 或 `某某老師借閱`）。
3. **書箱輪換星星開關（位於備註欄最右側，點擊即時切換）**：
   * 在清冊表格每一列的 **【備註】** 欄位**最右側**，皆設有一顆加大且可愛圓潤的 **星星圖示**：
     * **星星亮起（金色）**：代表該書箱正常**加入班際輪換**。
     * **星星熄滅（灰色）**：代表該書箱**暫不加入輪換**（例如：特定班級借閱中、有書本破損缺頁或修復中暫無法輪換）。
     * **極速切換與排程連動防護**：
       * 直接在表格中點擊星星圖示即可切換狀態；完成其他修改後，點左上角 LIBRARY 圖樣並確認儲存，才會正式儲存。
       * **已排程防護通知**：當您將已分派在班際輪換中的書箱星星點暗時，系統會**主動跳出通知提醒**；若確認取消輪換，系統將自動清除班際輪換排程以便重新分配；若未被分派則直接切換無須確認。
4. **修改或刪除書箱**：
   * 在清單中點擊欲修改的書箱列，右側表單會載入該筆資料。
   * 修改內容後點擊 **「儲存資料」** 即可直接更新（維持原本的星星輪換設定）；若要刪除該筆書箱，點擊 **「刪除書箱」** 即可。

### 二、班際輪換手動指定書箱

* 管理員點選班際輪換的書籍儲存格後，可直接輸入書箱編號指定書籍；只有實際輸入有效數字後才會建立手動指定。只點開後關閉，不會改動或指定書籍；若以 Backspace 清空既有書號後按 Enter 或移開游標，會刪除該指定書籍。
* 僅能輸入目前「書箱清冊」中確實存在的編號。若輸入不存在的書號，例如 `911`，系統會提示錯誤、保留原排程，且不會儲存或顯示未知書名。

---

### 二、 班際輪換系統模組

1. **學期切換與學期管理（右上角日曆圖示按鈕與下拉選單）**：
   * **切換學期**：直接點擊右上角的下拉選單，即可快速切換不同學期（如 `114學年度 第一學期`、`114學年度 第二學期`、`115學年度 第一學期`），系統會立即載入該學期的專屬輪換排程。
   * **115 學年度八年級直升黑名單排除機制**：系統已完整內建 114 學年度各班已讀歷史紀錄。當切換至 115 學年度八年級（801~812 班 / 201~212 班）時，電腦隨機分派已 100% 嚴格避開過去七年級已讀過的 6 本書籍，確保每位學生在國中階段絕不重複閱讀同一本書！
   * **學期管理（點擊日曆圖示按鈕）**：
     * 點擊下拉選單左側的 **📅 日曆符號**，即可開啟「學期管理」視窗。
     * **學期清單**：視窗內列出所有已建立之學期，**當前使用中的學期會以柔綠色底色標示**。
     * **刪除學期**：點擊學期右側的 **紅色垃圾桶符號**，確認後即可刪除該學期與其排程資料。
     * **新增學期**：在下方輸入區填寫數字（例如：`115` 學年度 第 `2` 學期），按下 **Enter 鍵** 或點擊 **「新增」**，系統將自動為新學期建立獨立資料並即時儲存切換。
2. **輪換系統設定（頂部齒輪圖示按鈕）**：
   * 在「班際輪換」頁面頂部大標題右側，設有一顆精緻的齒輪設定圖示按鈕。
   * 點擊齒輪按鈕即可開啟「輪換系統設定」對話視窗，一站式設定：
     * **班級數設定**：設定本學期七年級與八年級的班級數（例如：七年級 12 班、八年級 12 班）。
     * **輪換日程設定**：設定 1.發書（期初）、2.第一次交換、3.第二次交換、4.回收（期末）四項重要日期。
   * 設定完成後點擊 **「儲存設定」**，表格與報表抬頭日程將同步完成動態更新。
3. **切換年級**：
   * 點擊上方 **「7年級」** 或 **「8年級」** 按鈕，切換對應年級的班級輪換排程表。
   * 手機版會將排程改為「一班一張卡片」，卡片內依序顯示第一本、第二本與第三本；指定、清空、隨機、清除、列印與設定功能均與桌機一致。
4. **自由指定書籍（獨立鎖定）**：
   * 點擊表格中任何一個班級與階段（第一本、第二本、第三本）的儲存格，即可直接輸入書號（如 `5`）。
   * 輸入後該儲存格獨立鎖定為指定書籍（暖木底色 `#F5EFEA`，無框線），**不會自動更動或填入表格中的其他欄位與班級**。
   * 未輸入的儲存格**維持純淨完全空白**（不增加「未分配」等提示說明文字）。
   * 七年級首時段預設未指定時，僅簡潔顯示「**參觀圖書館＆新生齊步走**」，不加上多餘文字說明。
5. **一鍵跨年級聯合隨機分派（嚴格遵循星星開關規則）**：
   * 當您完成所有指定書籍設定後，點擊上方 **「隨機」** 按鈕。
   * **星星關閉排除規則**：若書箱清冊中某箱書的星星為關閉狀態（暫停輪換），輪換演算法將**自動排除該書箱**，絕對不會將其隨機分配給任何班級！
   * 電腦完全保留您手動設定的所有暖木色指定書籍卡片，**第一優先將有指定書籍的輪換動線完整排好（順向與逆向自動補齊同一條鏈上的其他階段）**，使書籍在班級間順暢移交、盡量不回流圖書館；若同一條鏈上有班級在七年級時已讀過該書，系統會智慧跳過該班並改派其他書籍。
   * 隨後，系統會為其餘全空的輪換鏈以隨機方式從「開啟星星」的合格書箱中挑選書籍，補齊柔綠色雙行移交說明卡片。
   * **移交機制自動檢查與圖書館回流**：運算完成後，若系統偵測到某些書籍因為指定中斷影響無法順移至下一個班級，系統會**自動將去向設定為【移交到圖書館】**，且**下個班級的來源亦會精準設為【來自圖書館】**；同時會自動彈出視窗詳細列出有問題的【年級】、【班級】與【階段/第一二三本】，方便您一目了然！
6. **清除按鈕（一鍵清理排程）**：
   * 點擊上方 **「清除」** 按鈕，系統會一鍵移除七年級與八年級的自動分配與教師來源資料，但保留管理員實際手動輸入的指定書籍，並重新計算其移交日期與路徑。
7. **螢幕畫面雙行移交說明顯示（水平置中、無外框）**：
   * 螢幕操作表格表頭維持「**第一本**」、「**第二本**」、「**第三本**」之簡潔抬頭。
   * 包含書籍之儲存格一律統一於儲存格內**水平置中**顯示雙行移交流轉卡片：
     * 行 1：`來自【上一個地點】的《書名》`
     * 行 2：`請於【規定時間】移交到【下一個地點】`
   * **教師指定書箱**：呈現暖木色底色（`#F5EFEA`），**無外框線**。
   * **隨機分配書箱**：呈現柔綠色底色（`#EAF0EC`），**無外框線**。
   * **七年級首時段**：若未指定書箱，極簡顯示「**參觀圖書館＆新生齊步走**」。
8. **一鍵列印與輸出 PDF 報表（七、八年級各一張獨立 A4 排程總覽表）**：
   * 點擊上方 **「列印」** 按鈕（或按快捷鍵 `Ctrl + P`），系統會立即同步產生全校「七年級」與「八年級」各一張獨立排程總覽表：
     * **表頭整合日期**：欄位標題直接顯示各階段日程（發書日、第一次交換、第二次交換），免除多餘重複文字。
     * **列印專屬四行格式**：紙張上每格資訊以標準四行換行呈現（來源、書名、移交指示、接收地點），排版清晰工整。
     * **第 1 頁**：七年級班級書箱輪換排程總覽表。
     * **第 2 頁**：八年級班級書箱輪換排程總覽表。

---

## 三、 常見問題與維護注意事項

* **問：如果換了一台電腦，資料還會在嗎？**
  * 答：目前資料儲存在個別電腦的瀏覽器中。若換電腦使用，建議複製整份資料夾，未來我們可為您擴充「匯出/匯入資料」功能，方便將資料帶著走。
* **問：輸入數字時不小心切到全形輸入法怎麼辦？**
  * 答：系統具備自動智慧轉換功能，輸入全形數字（如：１２３）時，系統會自動轉換為標準半形數字（123），請安心使用。
